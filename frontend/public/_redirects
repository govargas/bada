# Don't redirect these files - serve them directly
/robots.txt   /robots.txt   200

# TEMPORARY (cookie-topology spike): proxy the API through Netlify so the
# browser treats backend cookies as first-party to badaweb.netlify.app.
# Must precede the catch-all. Inert for current traffic (the app calls the
# absolute VITE_API_BASE URL). Remove after the spike unless we adopt topology A.
/api/*   https://bada-backend.vercel.app/api/:splat   200

# Catch-all: serve index.html for client-side routing
/*   /index.html   200