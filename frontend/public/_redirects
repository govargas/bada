# Don't redirect these files - serve them directly
/robots.txt   /robots.txt   200

# NOTE: the /api/* proxy now lives in netlify.toml as a *signed* redirect
# (topology A). It's kept out of this file so the signed rule isn't shadowed by
# an unsigned one here.

# Catch-all: serve index.html for client-side routing
/*   /index.html   200